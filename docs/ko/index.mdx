---
title: critical-script
description: TypeScript 모듈을 메인 JS 번들보다 먼저 실행되는 인라인 스크립트로 만드는 Vite 플러그인입니다.
template: splash
hero:
  tagline: TypeScript 모듈을 메인 번들보다 먼저 실행되는 인라인 스크립트로 만듭니다. API 프리페치와 리소스 프리로드, 웹뷰 네이티브 브리지, LCP 시간 단축에 사용합니다.
  actions:
    - text: 시작하기
      link: /critical-script/ko/getting-started
      icon: right-arrow
    - text: GitHub
      link: https://github.com/woowabros/critical-script
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components'
import LiveBenchmark from '../src/components/LiveBenchmark'
import Showcase from '../src/components/Showcase'

<Showcase client:load locale='ko' />

<CardGrid>
  <Card title='TypeScript로 작성' icon='seti:typescript'>
    컴파일 시점에 타입 검사를 받고, esbuild가 컴파일과 축소를 수행해 인라인 스크립트를 최대한 작게 유지합니다.
  </Card>
  <Card title='빌드 단계의 크기 제한' icon='approve-check'>
    기본값이 8192바이트인 `outputSizeLimit` 옵션은 인라인 스크립트가 한도를 넘으면 빌드가 실패하도록 합니다.
  </Card>
  <Card title='실행 시점이 중요한 작업' icon='rocket'>
    API 프리페치와 리소스 프리로드, 웹뷰 네이티브 브리지처럼 일찍 실행할수록 효과가 큰 작업을 위한 도구입니다.
  </Card>
  <Card title='프로덕션 사용' icon='star'>
    배달의민족 주요 웹뷰 지면에서 사용되며, API 프리페치 패턴으로 LCP 시간을 30~40% 단축했습니다.
  </Card>
</CardGrid>

{/* Hydrated when the reader reaches it, and it loads nothing until they press run. */}

<LiveBenchmark client:visible={{ rootMargin: '300px' }} locale='ko' />
