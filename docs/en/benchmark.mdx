---
title: Benchmark
description: See how quickly a page loads when critical-script handles the API request.
---

import BenchmarkStage from '../website/src/components/BenchmarkStage'

{/* `not-content` keeps Starlight's article styles off the benchmark's own headings and lists. */}

<div class='not-content'>
  <BenchmarkStage client:load locale='en' />
</div>
