---
// Replaces Starlight's theme provider, which would otherwise read a stored
// preference and switch `data-theme` to light on the client. Starlight already
// renders `data-theme="dark"` on <html>, so dropping the script pins it there.
---
